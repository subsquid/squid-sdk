{
  "changes": [
    {
      "packageName": "@subsquid/substrate-processor",
      "comment": "fix evm log fields in gateway request",
      "type": "patch"
    },
    {
      "packageName": "@subsquid/substrate-processor",
      "comment": "remove `id`, `pos`, `name` fields from data selection types as expected by the gateway",
      "type": "patch"
    }
  ],
  "packageName": "@subsquid/substrate-processor"
}
