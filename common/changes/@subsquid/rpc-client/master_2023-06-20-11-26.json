{
  "changes": [
    {
      "packageName": "@subsquid/rpc-client",
      "comment": "add `CallOptions.validateResult()` and `RetryError` for call result validation with ability to re-use built-in retry mechanism",
      "type": "minor"
    }
  ],
  "packageName": "@subsquid/rpc-client"
}