import {sts} from '../../pallet.support'

/**
 *  `:code` was updated.
 */
export type SystemCodeUpdatedEvent = null

export const SystemCodeUpdatedEvent: sts.Type<SystemCodeUpdatedEvent> = sts.unit()
