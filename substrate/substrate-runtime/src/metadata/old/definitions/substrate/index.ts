import {OldTypesBundle} from "../../types"
import {types} from "./types"
import {typesAlias} from "./typesAlias"


export const substrateBundle: OldTypesBundle = {
    types: types as any,
    typesAlias
}
