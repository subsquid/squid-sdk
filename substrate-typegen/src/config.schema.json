{
  "description": "Typegen config",
  "type": "object",
  "properties": {
    "outDir": {
      "description": "Output directory for generated files (relative to the config location)",
      "type": "string"
    },
    "chainVersions": {
      "description": "JSON file with information about chain spec versions (as generated by squid-substrate-metadata-explorer(1))",
      "type": "string"
    },
    "typesBundle": {
      "description": "JSON file with custom type definitions or name of a known chain. Required for chains containing blocks with metadata versions <14",
      "type": "string"
    },
    "events": {
      "description": "List of events to generate",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "calls": {
      "description": "List of calls to generate",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
