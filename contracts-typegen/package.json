{
  "name": "@subsquid/contracts-typegen",
  "version": "0.0.0",
  "description": "CLI for generating typescript types and decode implementations for the contracts pallet",
  "license": "GPL-3.0-or-later",
  "repository": "git@github.com:subsquid/squid.git",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "squid-contracts-typegen": "bin/run.js"
  },
  "scripts": {
    "build": "rm -rf lib && tsc"
  },
  "dependencies": {
    "commander": "^9.0.0",
    "@subsquid/util-internal-code-printer": "^0.0.2",
    "@subsquid/substrate-typegen": "^1.1.0",
    "@subsquid/contracts-metadata": "^0.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.22"
  }
}
